{
  "private": true,
  "name": "deployer",
  "dependencies": {
    "dotenv": "^2.0.0",
    "ioredis": "^1.15.1",
    "socket.io": "^1.4.5",
    "jsonwebtoken": "^7.0.0"
  },
  "scripts": {
    "postinstall": "touch ./node_modules/.install",
    "clean-build": "rm -rf public/build/ resources/assets/public/",
    "clean-deps": "rm -rf vendor/ node_modules/ resources/assets/node_modules",
    "install-deps": "composer install; npm install; cd resources/assets; npm install; cd ../.."
  }
}
